name: Build and Deploy
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and Build
        run: |
          npm ci
          npx ng build --output-path docs --base-href /tomfurlong.github.io/
          cp docs/index.html docs/404.html
      - name: Deploy
        uses: AhsanAyaz/angular-deploy-gh-pages-actions@v1.3.2
        with:
          secrets:
            ACCESS_TOKEN: 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
          github_access_token: ${{ ACCESS_TOKEN }} 
          build_configuration: application
          base_href: /tomfurlong.github.io/
          deploy_branch: gh-pages
          angular_dist_build_folder: docs